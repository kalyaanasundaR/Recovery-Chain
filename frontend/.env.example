# Copy to .env.local for dev, or set in the build environment / CI.

# API key sent as X-API-Key on mutating backend calls. Must match the backend's
# API_KEY. Baked into the bundle at build time, so treat it as a per-deployment
# shared secret, not a user secret.
VITE_API_KEY=test-api-key
